# FusionCareer V4 前端应用目录

这里是 Vue 3 + Vite 应用根目录。

完整的启动方式、目录说明、后端枚举、接口接入状态和新增功能契约统一维护在[仓库根目录 README](../../README.md)，本文件不再复制相同内容。

从当前目录启动：

```bash
npm ci
npm run dev
```

常用地址：

- 学生端：`http://localhost:5173/#/login`
- 管理端：`http://localhost:5173/#/admin`

如果终端显示了其他端口，请以 Vite 实际输出为准。
